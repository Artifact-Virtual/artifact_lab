```
// constants.js

const NOTE_TYPES = {
  TEXT: 'text',
  IMAGE: 'image'
};

const PRIORITY_LEVELS = {
  LOW: 'low',
  MEDIUM: 'medium',
  HIGH: 'high'
};

module.exports = {
  NOTE_TYPES,
  PRIORITY_LEVELS
};
```